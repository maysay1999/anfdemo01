docker images
docker run -it -d alpine /bin/sh
docker exec -it xxxxxxxxxxxx /bin/sh

dd if=/dev/zero of=100mb.dat bs=1024 count=102400
docker stop xxxxxxxxxxxx

dd if=/dev/zero of=100mb.dat bs=1024 count=102400
docker restart xxxxxxxxxxxx


##) Note
docker images
# is the same as 
docker image ls

docker ps
# is the same as
docker container ls