apiVersion: v1
kind: Pod
metadata:
  name: nginx-nfs-test
spec:
  containers:
  - name: nginx-nfs-test
    image: nginx
    resources:
      requests:
        cpu: 100m
        memory: 128Mi
      limits:
        cpu: 250m
        memory: 256Mi
