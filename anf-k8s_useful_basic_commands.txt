#curl
curl -O https://github.com/maysay1999/anfdemo01/blob/main/anf-k8s_useful_basic_commands.txt

# Details of NODE
kubectl get node -o wide
kubectl describe node
#status
kubectl get node -w

# Details of Pod
kubectl get pod -o wide
kubectl describe pod
#status
kubectl get pod -w

#emptyDir
# container w/o PV
kubectl run nginx-empty --image=nginx

#Persistent Volume
kubectl apply -f anf-pv-nfs.yaml
kubectl describe pv pv-nfs

#Persistent Volume Claim
kubectl apply -f anf-pvc-nfs.yaml
kubectl describe pv pv-nfs

apt update
apt install curl
apt install vim
dd if=/dev/urandom of=test128m.dat bs=1M count=128
